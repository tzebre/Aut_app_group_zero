name: GitHub push
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: pipreqs --force  ./
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - run : docker build -t python-imdb .